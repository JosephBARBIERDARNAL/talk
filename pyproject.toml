[project]
name = "talk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "jupyter",
    "nbclient",
    "nbformat",
    "numpy",
    "pyyaml>=6.0.2",
]

[tool.ruff]
extend-exclude = ["src/pycon2025/index.qmd"]
