[project]
name = "talk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "jupyter",
    "nbclient",
    "nbformat",
    "numpy",
]

[tool.ruff]
extend-exclude = ["src/pycon2025/index.qmd"]
